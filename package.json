{
  "name": "@pickme/report",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "build:single-spa": "vite build --config vite.single-spa.config.ts",
    "test": "echo \"Error: no test specified\"",
    "preview": "vite preview",
    "lint": "npx steiger ./src",
    "start": "npm run build && npm run build:single-spa && npm run preview"
  },
  "devDependencies": {
    "@types/node": "^22.9.3",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vite-plugin-single-spa": "^0.7.0",
    "vite-tsconfig-paths": "^5.1.3",
    "vue-tsc": "^2.1.10"
  },
  "dependencies": {
    "single-spa": "^6.0.3",
    "vue": "^3.5.13"
  }
}
